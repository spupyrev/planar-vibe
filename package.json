{
  "name": "planar-vibe",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "report:data": "timeout 10m node scripts/compute-report-data.mjs",
    "report": "timeout 2m node scripts/generate-report.mjs"
  }
}
